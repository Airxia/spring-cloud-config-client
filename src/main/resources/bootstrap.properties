### bootstrap 上下文配置
# 配置服务器URI
spring.cloud.config.uri=http://localhost:9090/
# 配置客户端名称
spring.cloud.config.name = jarluo
# profile是激活配置
spring.cloud.config.profile=prod
# label在git中指的是分支名称
spring.cloud.config.label=master

